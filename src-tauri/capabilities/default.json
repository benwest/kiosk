{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the kiosk and config windows",
  "windows": ["main", "config"],
  "permissions": [
    "core:default"
  ]
}
